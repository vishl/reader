App.Models.Forum = Backbone.Model.extend({
    urlRoot: 'forums'
    /*
    url: function(){
      var path = 'documents';
      if(!this.isNew()) {
        path+=this.sid;
      }
      return path;
    }
    */
})

