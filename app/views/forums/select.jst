Select a Fread:
<form>
  <select id="forum-select">
    <%for(var i=0; i<subscriptions.length; i++){%>
      <option value="<%=subscriptions.at(i).id%>"
      <%= subscriptions.at(i).id===selected ? 'selected="selected"' : ''%>
      ><%=subscriptions.at(i).get("title")%></option>
    <%}%>
  </select>
</form>
